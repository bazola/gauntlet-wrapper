# Gauntlet Resume -- {{PROJECT_DISPLAY_NAME}}

This project has already been onboarded and kicked off in a previous session. You don't
need to repeat discovery or draft the binding-contract docs again.

Current generation: {{CURRENT_GENERATION}}.

**If this repo has an older, pre-wrapper `progress/` directory at repo root (e.g.
`progress/state.json`, `progress/index.html`) -- that is superseded. Do not update it,
even if your own past habit in this project was to.** Every round's summary belongs
only under `.gauntlet/progress/`; the wrapper's Progress tab reads nothing else, so
writing a round's summary anywhere else means it silently never shows up there at all.

Before continuing the loop:

0. Re-read this project's binding docs -- the architecture/feel/evidence-style bars from
   your original kickoff's discovery, formalized in `.gauntlet/config.json`'s
   `projectDocs` array so a fresh session doesn't have to rediscover them from scratch:

{{PROJECT_DOCS_LIST}}

   If that list is empty (an older project, onboarded before this field existed),
   identify this project's binding docs now, re-read them, and populate `projectDocs`
   before continuing -- don't proceed on memory of a conversation you're not actually in.
1. Re-read `.gauntlet/config.json` -- `reviewerModel` may have changed since your last
   session; always use the live value when spawning critic subagents.
2. Re-read `.gauntlet/references/catalog.json` -- new references may have been added.
   **If any `gameplay[]` entry has `status: "raw"`, formalize it now, before doing
   anything else** -- same process as your original kickoff's S4 (draft testing
   requirement(s) into `.gauntlet/progress/requirements.json`, then set the source
   reference's `status: "formalized"`, `formalizedAt`, and `derivedRequirementIds`).
   This is what makes the requirement show up in the wrapper's References tab as
   something the human can see is now actually being tested -- an entry left `raw`
   stays invisible to the loop and to them.
3. Re-read `.gauntlet/goal.md` -- it may have been edited.

Then continue the gauntlet loop: build, capture, judge, triage and fix the round's top 3
gaps, repeat, per the rules in your original kickoff (S5-S9 there still apply in full).
